hosts: localhost
remote_user: root

tasks:
  - name: ensure apache is at the latest version
    register: out
    find:
       paths: "/tmp/source"
       patterns: "*.yml"
  - debug: var=out.stdout_lines
