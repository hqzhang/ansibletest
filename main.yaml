---
- name: Ansible  Example
  hosts: localhost
  vars:
    data: "ab,cd"
    cond1: "'abc' in out.stdout"
    cond2: "'cde' in out.stdout"
    output_variable:
      - abc
      - cde
    condition_variable:
      - "'abc' in out.stdout"
      - "'cde' in out.stdout"
  tasks:
    - shell: echo {{output_variable}}
      register: out
      until: 
        - cond1
        - cond2
      retries: 10
      delay: 2
    
   

  